# 프로젝트 작업 규칙

- 이 프로젝트에서는 요청받은 수정과 필요한 검증이 완료되면 별도 확인 없이 자동으로 커밋한다.
- 커밋에는 해당 작업에서 완료한 변경만 포함하고, 다른 작업의 변경은 섞지 않는다.
- 커밋 메시지는 한국어로 작성하며 명령형 표현은 사용하지 않는다.
- "jar 만들어줘" 요청 시 루트 `build-jar.ps1`로 테스트와 빌드를 실행하고 `deploy/homephoto-server.jar` 및 체크섬을 함께 커밋한다. 푸시는 사용자가 직접 한다.
